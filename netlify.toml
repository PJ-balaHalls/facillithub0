[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Garante que o motor de IA seja incluído no empacotamento das funções
  included_files = ["src/lib/ai-finder.ts"]