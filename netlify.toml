[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  included_files = ["src/lib/ai-finder.ts"]
  # Aumenta o tempo limite para o processamento da IA
  timeout = 60